# BACKLOG

- Add streaming encrypt/decrypt APIs for very large files.
- Add a concrete qclib-based PQC provider implementation once target API is finalized.
- Add file-level CLI commands (`z-crypto encrypt/decrypt`) for each mode.
- Add recursive directory sync helpers to `z.ftp` with retry/backoff policies.
- Add `zi cipher decrypt` and key rotation support with secure private-key recovery.
